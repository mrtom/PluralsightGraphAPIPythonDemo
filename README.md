Server side access to the Graph API
===================================

This is a very simple app which shows how to use the Graph API in your application using Python and the Graph API directly (no SDKs)
